// Copy this file to config.js and add it to index.html before app.js when your
// Supabase project is ready. These are public browser values, never admin keys.
window.RENTIN_CONFIG = {
  supabaseUrl: 'https://your-project.supabase.co',
  supabaseAnonKey: 'your-public-anon-key',
}
